import { h } from "preact";
import { RoutePage } from "../interfaces";

export const SettingsPage = (props: RoutePage) => {

    return (
        <div>
            settings page
        </div>
    )
}