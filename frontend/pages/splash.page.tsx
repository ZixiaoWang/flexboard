import { h } from "preact";
import { SplashLogoComponent } from "../components";

export const SplashPage = () => {
    return (
        <div className="is-full-screen is-all-center bg-blue60">
            <SplashLogoComponent />
        </div>
    )
}